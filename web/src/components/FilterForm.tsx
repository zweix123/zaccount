import './FilterForm.css'

function FilterForm() {
  return (
    <div className="filter-form">
      <label className="filter-form-label">筛选条件</label>
      <div className="filter-form-content">
        {/* 表单内容暂时为空 */}
      </div>
    </div>
  )
}

export default FilterForm

