[project]
name = "zaccount"
version = "0.1.0"
description = "A local-first personal ledger with safe CSV storage"
authors = [{ name = "zweix123", email = "1****************@user.noreply.gitee.com" }]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openpyxl>=3.1.5",
    "commentjson>=0.9.0",
    "python-dotenv>=1.0.0",
    "fastapi>=0.140.0",
    "uvicorn[standard]>=0.51.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
    "rich>=14.0.0",
    "types-openpyxl>=3.1.5.20250919",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
